// Layout para rotas públicas (login, recuperar senha)
// Transparente — cada página define seu próprio fundo e estrutura
export default function PublicLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
