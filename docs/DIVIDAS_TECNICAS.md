# Dividas Tecnicas — Cachola OS

Fonte de verdade do backlog de dividas tecnicas e pendencias do Cachola OS. Mantido em conjunto pelo Claude (consultor, no chat) e pelo Claude Code (executor). Conforme cada item for resolvido, mova-o para a secao "Resolvidas" no final, com a data e a versao — nao apagar de vez, pra manter o historico. Nao depender da memoria do chat: este arquivo e o registro permanente.

Status possiveis: PENDENTE | BLOQUEADA | AGUARDANDO TERCEIRO | VERIFICAR

## Infraestrutura / DevOps

- [x] (18/06/2026, v1.58.2) Esteira de migracao com botao (workflow_dispatch no GitHub Actions): primeira migration real aplicada com sucesso via esteira em producao (migration 160 — show_in_main_calendar em maintenance_executions). Workflow migrate-prod.yml operacional: backup pg_dump pre-migracao, transacao --single-transaction, healthcheck em cachola.cloud, log em cachola_migration_log. Fluxo validado end-to-end: deploy leva o .sql → esteira aplica no banco. PROCESSO: migration aplicada pela esteira em producao NAO chega automaticamente ao banco de dev — apos cada aplicacao via esteira, aplicar tambem no dev com: PGPASSWORD=... docker exec -e PGPASSWORD=... cacholaos-db psql -U supabase_admin -d postgres < migrations/NNN_*.sql && docker exec cacholaos-db psql -U postgres -d postgres -c "NOTIFY pgrst, 'reload schema';". O dono da tabela no dev e supabase_admin (nao postgres); usar -U supabase_admin.
- [x] (23/06/2026) Janela de manutencao da VPS de producao: upgrade de Docker/containerd/snapd; corrigir a causa-raiz do LimitNOFILE do dockerd (reseta para 65535 no reboot e quebra o supabase-pooler — fix via ulimits nofile no supavisor do docker-compose); autoremove dos kernels antigos (6.8.0-111 e 6.8.0-117) apos confirmar estabilidade do 6.8.0-124; versionar o docker-compose.yml do Supabase no git. STATUS: PENDENTE (parcial). SUB-PARTES RESOLVIDAS (23/06/2026, commit 87e272f, em develop): (a) "versionar o docker-compose.yml do Supabase no git" FEITO — capturado em `infra/prod-supabase/` (o Supabase de prod e clone de supabase/supabase @ 4189e763 com 6 customizacoes locais; versionado como README + docker-compose.custom.patch das 22 linhas custom + .env.example mascarado; reconstrucao deterministica de DR). (b) o fix de `ulimits.nofile` do supavisor JA ESTA no compose de prod (descoberto na captura) — a parte do LimitNOFILE no nivel do docker-compose esta feita; resta so a causa-raiz no nivel do dockerd/systemd. DIAGNOSTICO 23/06/2026 (LimitNOFILE): confirmado que o pooler JA ESTA PROTEGIDO — o container supabase-pooler reporta nofile 100000/1048576 (do `ulimits` do compose), enquanto o dockerd usa o default 65535 (sem LimitNOFILE explicito no docker.service). Como o `ulimits` do compose re-aplica a cada (re)start do container, um REBOOT NAO quebra o pooler — o risco original esta MITIGADO pelo fix do compose (ja versionado em infra/prod-supabase). RESTAM PENDENTES (todos para a MESMA janela de manutencao, que ja exige reboot): upgrade Docker/containerd/snapd; autoremove kernels 6.8.0-111/117; OPCIONAL (defense-in-depth) drop-in systemd `LimitNOFILE=1048576` no docker.service (cobre o daemon e containers SEM ulimits explicito) — fazer JUNTO do reboot da janela, nao standalone (evita downtime extra). Pre-req da janela: snapshot Hostinger antes (regra de ouro da vps-ops). CORRECAO 23/06/2026: o "remover o leftover /opt/cacholaapp/docker-compose.yml" foi RETIRADO do escopo — esse arquivo e TRACKED no repo (e o compose de DEV, presente na prod so porque ela tem o repo inteiro em /opt/cacholaapp); um `rm` no disco da prod quebraria o gate de working-tree do deploy.yml (licao do INC-001). E inofensivo (nenhum container o usa); nao mexer. ESTIMATIVA da janela: ~45-60 min de wall-clock, ~5-10 min de downtime real (restart do docker no upgrade + reboot). Fazer em horario de baixo trafego (off-hours do buffet). >>> EXECUTADA 23/06/2026 ~19h BRT (snapshot Hostinger criado 19:01, expira 13/07, restauracao ~30m). Resultado: (1) Docker/containerd/snapd/kernel SEM upgrade disponivel (ja na ultima — Docker 29.6.0, containerd v2.2.5); apt upgrade geral do Ubuntu NAO feito (fora de escopo; ~2-5 security updates deferidos). (2) Drop-in `/etc/systemd/system/docker.service.d/nofile.conf` (LimitNOFILE=1048576) criado + daemon-reload. (3) Kernel: Bruno escolheu REMOVER o 6.8.0-117 (Opcao A); purgados o 117 (image/modules/headers/tools) + residuos rc 106/107/110/111 (14 pacotes, 190 MB liberados, update-grub auto); so o 6.8.0-124 restou como bootavel. (4) Reboot. POS-REBOOT VERIFICADO: kernel 6.8.0-124, 13 containers supabase Up/healthy, **dockerd nofile agora 1048576** (drop-in OK), pooler 100000/1048576, PM2 cacholaos online, build-info 890e757. Downtime real ~1-2 min. Sobra inofensiva: pastas vazias /lib/modules/6.8.0-106|107 (limpaveis com rm -rf) + 1 zombie process transitorio. STATUS: RESOLVIDA.
- [x] Auditoria de vulnerabilidades npm / Dependabot — TRIAGEM FEITA em 22/06/2026 (branch chore/deps-security-high). 79 alertas do Dependabot -> 20 pacotes distintos -> so 3 dependencias diretas (next, nodemailer, xlsx). Classificacao por exposicao real:
  - BUMPADO (runtime-facing real, validado tsc+lint+build): next 16.2.1->16.2.6 (14 high do core), nodemailer ^6.9.0->^6.10.1 (ReDoS GHSA-9h6g, unico fix dentro do major 6), @aws-sdk/client-s3 + s3-request-presigner ^3.1033->^3.1073 (corrige fast-xml-parser 5.5.8->5.7.3).
  - 2a PASSADA (overrides cirurgicos, 22/06, branch chore/deps-overrides-lowrisk, validado tsc+build): dompurify -> ^3.4.11 (fecha ~12; via jspdf, nao usamos .html()) e fast-uri -> ^3.1.2 (fecha o high residual; via @aws-sdk xml). npm audit 26 -> 21.
  - RISCO ACEITO / sem fix limpo (documentado, sem acao): xlsx (sem fix npm; so escrita/export, sem XLSX.read de input nao-confiavel); hono+@hono/node-server (~21, via shadcn — que e build-dep REAL, globals.css faz @import "shadcn/tailwind.css"; vulns sao de web-server do hono, inalcancaveis por CSS estatico); lodash (advisory pede 4.18.x; override quebraria workbox — build-time, exposicao ~zero); brace-expansion (3 majors na arvore, override unico quebraria consumidores; ReDoS build-time); serialize-javascript/ws@7.5.10/babel/esbuild/postcss/js-yaml/qs/ip-address (tooling de build, nao roda em producao).
  - DEFERIDO p/ migracao major dedicada (com teste): (a) @supabase/supabase-js 2.108.x endurece tipagem insert/update e quebra tsc em ~9 call-sites — exige ajuste de codigo, nao e bump seguro; o ws transitivo do realtime tem baixa exposicao (somos cliente ws); (b) nodemailer 6->9 p/ o high GHSA-p6gq (raw bypass) + CRLF — baixa exposicao (so com input controlado pelo atacante, que nao expomos). Regra mantida: NUNCA npm audit fix --force. RESULTADO em prod (22/06/2026): 79 -> 45 alertas (todos os bumps seguros aplicados e deployados). DECISAO do Bruno (22/06/2026): os ganhos seguros se esgotaram; os 45 restantes sao REVISADOS E SOB CONTROLE, sem acao de baixo risco pendente — sao build-tooling (nao roda em prod) + nao-exploraveis no nosso uso (xlsx write-only, hono via shadcn CSS) + 1 migracao major opcional (nodemailer 6->9, runtime real mas baixa exposicao: so com input controlado pelo atacante, que nao expomos). STATUS: TRIADO E ESTAVEL (reabrir so se: (a) decidirmos investir na migracao major do nodemailer 9 com teste de e-mail, ou (b) surgir um alerta novo de exposicao real). NAO e mais item de baixo esforco. >>> RESOLVIDO 25/06/2026 (prod 2b24ced): os 8 HIGH foram a zero — a migracao major do nodemailer 6->9 foi feita e validada (smoke + security-reviewer), os "risco aceito" viraram fix real (overrides hono/babel/serialize-javascript/lodash/ws) e o xlsx subiu p/ 0.20.3 via CDN do SheetJS. Ver o item correspondente em Resolvidas (25/06) + a divida nova "dependencia do CDN do SheetJS" abaixo.
- [x] (23/06/2026) Auditar paridade de migracoes local vs producao. VERIFICADO — SEM DRIFT: repo, DEV e PROD todos na migration 162 (ultima aplicavel; numeracao 1-162 sem gaps; +70 arquivos de rollback que nao sao aplicados). Era-esteira: `cachola_migration_log` da prod lista 160/161/162 (aplicadas por suportedrk via migrate-prod.yml) e o probe de schema confirma as colunas das 3 (show_in_main_calendar, decorator_name, corkage_quantity = t). DEV idem (probado em cacholaos-db). Pre-esteira (1-159) implicitamente aplicadas (app em producao funcional + a era-esteira constroi sobre elas). DESCOBERTA: o `deploy.yml` NAO aplica migrations automaticamente (a skill migrations.md estava DESATUALIZADA nesse ponto) — migrations chegam so por manual `docker exec psql` (ate 159) ou esteira (160+); nao ha rede automatica, entao a disciplina deploy->esteira e o unico mecanismo de chegada. Sem acao.
- [ ] **Dependencia de build no CDN do SheetJS** (introduzida 25/06/2026 ao corrigir os 2 HIGH do xlsx). O `package.json` aponta o xlsx para `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` (o npm registry so tem a 0.18.5 abandonada/vulneravel). Consequencia: `npm ci` no CI e no deploy da VPS busca o xlsx do `cdn.sheetjs.com` — **se o CDN ficar fora do ar, o build/deploy quebra** (e prod fica intacta pelo gate de working-tree, mas o deploy falha). MITIGACAO POSSIVEL se virar problema: hospedar o tarball num storage proprio (ex.: bucket R2) e apontar o package.json para la, ou vendorar o .tgz no repo. STATUS: ACEITO (monitorar; o CDN do SheetJS e estavel e e o metodo oficial recomendado por eles). Ao subir versao do xlsx no futuro, trocar a URL para a nova (xlsx-X.Y.Z.tgz) — nao adianta `npm update`.
- [ ] **Deploy nao-atomico gera erros transitorios na janela de build** (identificado 25/06/2026 via 2 alertas do Sentry no deploy da v1.68.0). O deploy faz `next build` NO LUGAR: o `.next/` e reescrito enquanto o processo PM2 antigo ainda atende, entao por ~3-5 min (agravado pelo retry do throttle SSH) o processo velho nao acha chunks/manifests que o build sobrescreveu → `Could not find the module ".../next/dist/lib/..."` e `InvariantError: client reference manifest for route "..." does not exist`. **Some sozinho** quando o PM2 reinicia no build novo (confirmado: todos os erros foram ANTES de o deploy fechar; rotas todas 307/healthy depois; build-info no commit novo). NAO e bug do app. MITIGACAO APLICADA (25/06/2026, sem bump): filtro no `beforeSend` compartilhado (`src/lib/observability/scrub-pii.ts` — `isTransientBuildArtifact`) descarta esses 2 padroes ANTES do scrub de PII, pra nao afogar o sinal real do Sentry (fadiga de alerta). Escopo seguro: o padrao do "module" exige `next/dist/` → erro de modulo do nosso codigo NAO e silenciado; se um deploy quebrar de verdade, o healthcheck do deploy.yml falha e o build-info nao avanca (caminho real coberto). CORRECAO-RAIZ FUTURA (opcional, maior): deploy atomico/blue-green (build num dir novo → swap de symlink → `pm2 reload`), que elimina tanto o ruido quanto eventuais 500 que usuarios reais peguem na janela. STATUS: RUIDO MITIGADO; correcao-raiz ACEITA como baixa prioridade (so se a janela de deploy comecar a impactar usuario real ou se o blue-green virar prioridade de infra).
- [ ] Limpar o diretorio untracked scripts/ops/ que aparece na working tree da VPS de producao. STATUS: PENDENTE (baixa prioridade).
- [ ] Esteira de migracao Fase 2: modo "aplicar pendentes" (varrer supabase/migrations, comparar com a tabela cachola_migration_log e aplicar em ordem o que falta, cada uma em sua propria transacao) + backfill da tabela com as migrations historicas ja aplicadas. STATUS: PENDENTE (fazer depois que a Fase 1 rodar algumas migrations reais).
- [x] (26/06/2026, develop — ativo em prod no proximo merge p/ main) Esteira (polimento opcional): "Validar inputs" do migrate-prod.yml virou um job separado `validar` SEM `environment`, e o job `migrate` (gate `producao`) ganhou `needs: validar`. Agora nome de arquivo invalido / confirmacao errada falha ANTES do gate de aprovacao (antes precisava aprovar o environment so para entao ver o erro). YAML validado. So workflow+doc (sem codigo de app) → nao justifica deploy dedicado; entra em vigor quando o `migrate-prod.yml` for lido de `main` (proximo release). Esteira roda sempre com `ref: main`.
- [ ] Claude Code (executor) na VPS de dev nao consegue verificar o status do CI nem ler o repo via API: nao ha gh CLI nem token GitHub configurado, e o WebFetch nao acessa repositorio privado. Hoje, antes de cada merge para main, o Bruno precisa confirmar manualmente o CI verde no GitHub. Avaliar: configurar gh CLI + token de leitura (escopo minimo repo:read / actions:read) como secret na VPS de dev, ou expor o status do CI por um endpoint/proxy. STATUS: PENDENTE (identificado em 19/06/2026, durante a Frente A v1.60.0). NOTA (20/06/2026): RESOLVIDO via MCP(1) abaixo — gh autenticado com token read-only; o Claude ja le o status do CI sozinho. Ver item (1) dos "MCPs de apoio aos subagentes".

## Subagentes / Tooling de qualidade

> Suite de subagentes (auditores read-only + 1 que escreve testes) criada em 20/06/2026 em `.claude/agents/`, versionada no git. **9 agentes prontos** (atualizado 22/06/2026): rbac-auditor, migration-reviewer, ploomes-verifier, security-reviewer, visual-qa, test-author, **db-performance-reviewer** (7o, criado 22/06), **data-fetching-reviewer** (novo 22/06 — pega o "Skeleton Loading Infinito"), **design-tokens-reviewer** (novo 22/06 — par estatico do visual-qa). Os itens abaixo sao o que ficou de fora dessa leva, de propria escolha ou por serem opcionais.

- [x] (22/06/2026) 7o subagente — db-performance-reviewer (revisor de indices e custo de politicas RLS em escala) CRIADO. Estava adiado de proposito (prematuro com tabelas pequenas); criado mesmo assim por decisao do Bruno — o custo de criar e baixo (so a definicao .md) e ele fica pronto p/ disparar quando mexer em indice/RLS/RPC de BI, rendendo mais conforme o banco cresce. Caveat embutido: dev e snapshot pequeno -> EXPLAIN engana, raciocina por escala. Bonus na mesma leva: criados tambem **data-fetching-reviewer** (audita padroes de TanStack Query, pega o "Skeleton Loading Infinito") e **design-tokens-reviewer** (design system no nivel de codigo: hex hardcoded, oklch em print, Button asChild). Total agora: 9 subagentes. AGENTS.md atualizado.
- [ ] MCPs de apoio ao executor/subagentes — 3 recomendados em 20/06/2026: (1) **GitHub MCP** — **FEITO em 20/06/2026** via gh CLI autenticado com token fine-grained read-only (Issues/Actions/Contents/PRs/Dependabot alerts: Read), guardado em ~/.config/gh na VPS de dev (fora do repo). O Claude ja le Issues, alertas do Dependabot e o status do CI sozinho. Pendencias menores: secret-scanning de repo privado e pago (pulado); MCP server formal e opcional (o gh ja cobre). Mover para Resolvidas quando consolidado. (2) **Supabase/Postgres MCP** no lado do executor — pros agentes auditarem RLS/permissoes contra os dados reais (hoje dependem de workaround via psql). (3) **Sentry MCP** (observabilidade) — monitor de erros de producao; hoje e ZERO, um erro que atinja um cliente nao gera nenhum alerta. NOTA (23/06/2026): a observabilidade em si foi ENDERECADA — Sentry SaaS (free tier) integrado via @sentry/nextjs v10 (Fase 1: captura de erro server/edge/client + scrub de PII no beforeSend para LGPD; desligado fora de prod; release alinhada ao BUILD_ID), EM PRODUCAO desde 23/06/2026 (merge 3219a53; deploy verde; DSN no `.env` da prod; o build carimba `NEXT_PUBLIC_SENTRY_DSN` e o runtime le `SENTRY_DSN`; deprecation warnings da v10 ja movidos para `webpack.*`). VALIDADO end-to-end: smoke `environment=production` entregue ao painel (eventId 82f7d8a8…) — caminho prod->Sentry confirmado; erros reais do app caem automaticamente. Fase 2 (sourcemaps) FEITA 23/06/2026: `SENTRY_ORG=buffet-cachola` + `SENTRY_PROJECT=javascript-nextjs` + Organization Token `SENTRY_AUTH_TOKEN` adicionados ao .env da prod; rebuild subiu 520 sourcemaps (release 890e757, via debug IDs; deleteSourcemapsAfterUpload=true por padrao -> maps deletados do build, nao ficam publicos). Stacks de erro reais agora vem legiveis (arquivo:linha). Zero codigo novo (a fiacao do withSentryConfig org/project/authToken ja existia da Fase 1). OBSERVABILIDADE SENTRY 100% COMPLETA (captura + sourcemaps). O "Sentry MCP" (acesso do AGENTE aos erros) continua opcional/futuro — distinto da observabilidade, que ja esta resolvida. NOTA: o **Chrome DevTools MCP** (o 4o do plano) JA EXISTE e e usado pelo subagente visual-qa — nao e pendencia. STATUS: PENDENTE.
- [x] (22/06/2026) Dependabot — scan concluido e alertas TRIADOS. Ver item consolidado "Auditoria de vulnerabilidades npm / Dependabot" na secao Infraestrutura/DevOps acima (79 alertas -> 20 pacotes -> baldes runtime/nao-exploravel/tooling/deferido; bumps de alta prioridade feitos no branch chore/deps-security-high). Dependabot security updates (auto-PRs) segue DESLIGADO de proposito.
- [ ] gh/GitHub na VPS de dev — autenticado em 20/06/2026 com token fine-grained READ-ONLY (em ~/.config/gh, NUNCA no repo). O token tem validade (~90 dias). Quando expirar, o gh para de autenticar e o Claude perde a visao do GitHub (Issues/CI/Dependabot) — sintoma: `gh auth status` diz "not logged in" ou chamadas dao 401. Correcao: Bruno gera novo token read-only e refaz `printf %s '<token>' | gh auth login --with-token`. STATUS: nota operacional (sem acao ate expirar).

## RBAC / Permissoes

- [x] (22/06/2026) RBAC — applyRoleTemplate fire-and-forget (use-units.ts): resolvido o silencio. O `.catch(()=>{})` virou `.then(check res.ok).catch(toast.warning)` — em falha do POST /api/admin/users/[id]/apply-role-template, o admin agora recebe aviso (10s) apontando para reaplicar o template na tela de permissoes do usuario (o botao "Aplicar template do cargo" ja existe la desde o PR3 — nao precisou criar). Revisado pelo rbac-auditor (mudanca puramente aditiva de UX, nao altera permissao efetiva nem o guard ADMIN_USERS_MANAGE_ROLES). Retry automatico nao implementado (o aviso + botao manual cobrem o caso). Identificado em 18/06/2026, release v1.58.0.
- [x] (23/06/2026) Atas — escrita via check_permission. VERIFICADO (ja estava feito, migrations 044 -> 077/153, ATAS_MANAGE_ROLES nem existe mais no codigo): as 3 tabelas (meeting_minutes / meeting_participants / meeting_action_items) exigem `check_permission(auth.uid(),'atas', create|edit|delete)` no RLS de escrita — em AND, nao bypass (`created_by`/`is_diretoria()` sao apenas row-scope, nao concedem permissao). A camada de app usa `useAtasPermissions()` que chama o RPC `check_permission` (create/edit/delete). `role_permissions` concede create/edit/delete a diretor+gerente (super_admin tambem export); operacionais tem so view — a infra esta pronta p/ dar escrita a um cargo operacional bastando flipar o template (as entradas NAO sao "ocas"). Backfill coberto pelo applyRoleTemplate no fluxo de convite/troca de cargo (e qualquer drift agora e detectavel pelo Fix de orfas ad0b769). Sem acao de codigo.
- [x] (23/06/2026) Manutencao — template do cargo DIRETOR. VERIFICADO: o template `role_permissions[diretor][manutencao]` ja tem `view/create/edit/delete/approve/export` todos granted=true — a reforma da v1.55.0 cobriu. Um diretor novo herda a gestao de manutencao automaticamente. Sem acao.
- [x] (23/06/2026, prod e274e70) RBAC — troca de cargo via applyRoleTemplate (src/lib/rbac/apply-template.ts) fazia apenas UPSERT das linhas do template do novo cargo e NAO removia grants do cargo anterior que o novo nao menciona. Como role_permissions e esparso (so lista linhas explicitas, nao a grade completa de modulo x acao), trocar um usuario de cargo X para Y deixa permissoes orfas de X — o usuario fica sobre-permissionado em relacao ao novo cargo. Agravante: a API role-template-diff (src/app/api/admin/users/[id]/role-template-diff/route.ts) so itera as linhas do template, entao essas orfas ficam INVISIVEIS no diff da tela /admin/usuarios/[id]/permissoes. Correcao sugerida: (a) na troca de cargo, zerar as permissoes globais do usuario antes do upsert (resultado = exatamente o template); (b) no diff, listar tambem as linhas de user_permissions ausentes no template (orfas) como "a remover". RESOLVIDO (commit ad0b769, merge adf171e, em prod 23/06/2026): (a) applyRoleTemplate ganhou opts.prune que remove as orfas (escopo user_id+unit_id; upsert antes da poda; super_admin via SYSTEM_ONLY_ROLES nao e podado), aplicado em change-role e no botao "Aplicar template do cargo"; (b) o diff agora lista as orfas como "Remover" (antes invisiveis). Revisado pelo rbac-auditor; validado em dev (toast "1 orfa removida") e deployado. apply-to-all mantido como merge (sem prune) de proposito. (identificado em 19/06/2026, correcao de cargo da Regina freelancer -> pos_vendas).
- [x] (23/06/2026, prod) RBAC — drift de permissoes individuais da Vitoria (pos_vendas). RESOLVIDO: com o Fix de grants orfaos em prod (commit ad0b769), o diff de /admin/usuarios/[id]/permissoes expos as 6 orfas (eventos.export ✓, eventos.create/edit/delete sem grant, atas.create ✓, atas.edit ✓) como "Remover"; o Bruno aplicou o template do cargo em 23/06, podando-as — a Vitoria agora bate exatamente com o template pos_vendas. Observacao para o futuro: a deteccao de drift de OUTROS usuarios ainda nao tem varredura em massa (so e visivel abrindo a tela de cada usuario); se virar necessidade, criar um relatorio que liste todos os user_permissions fora do template do cargo. (identificado em 19/06/2026).

## BI / Dados

- [x] (23/06/2026) BI — divergencia de unidade raw-vs-canonica em get_bi_sales_metrics e get_bi_unit_comparison. VERIFICADO via pg_get_functiondef: ambas ja usam a unidade CANONICA — `COALESCE(e.unit_id, pd.unit_id)` para won/tempos (festa 1-por-deal) e `pop.unit_id` para receita (festa por-produto). get_bi_unit_comparison usa `pd.unit_id` (pretendida) so para leads/denominador da conversao, por design documentado. Sem divergencia remanescente; mig 151/154 cobriram. Sem acao.
- [x] (23/06/2026) BI backlog (3 ajustes). (1) Antecedencia media respeitando o periodo: **JA ESTAVA FEITO** (debito desatualizado) — `bi/page.tsx` usa `periodAdvance`, media ponderada de `avg_booking_advance_days` por `won_deals` sobre o periodo selecionado. (2) Badges de delta nos KPI cards: **JA ESTAVA FEITO** — os 4 KpiCards recebem `trend={trend*}` e o card de Antecedencia tem `<TrendBadge trend={trendAdvance}>`; os `prev_*` ja vem das RPCs `get_bi_sales_metrics`/`get_bi_conversion_data` (params `p_prev_start`/`p_prev_end`), e `bi/page.tsx` ja calcula `calcTrendPct(periodo, prev)`. (3) Drilldown da vendedora alinhado ao periodo: **CORRIGIDO 23/06** — `seller-drilldown-sheet.tsx` mostrava `avg_days_to_close` so do ULTIMO mes; agora exibe a media ponderada (por `won_count`) de TODO o periodo selecionado + texto do InfoPopover ajustado ("media ponderada de todo o periodo selecionado"). Frontend-only, sem migration; tsc/lint OK; validado pelo subagente visual-qa. STATUS: RESOLVIDO (em prod 890e757, 23/06/2026). Nota: a secao "DEBITOS TECNICOS / BI Pendentes da v1.7.0" do CLAUDE.md esta desatualizada (itens 1 e 2 ja feitos) — atualizar quando tocar o CLAUDE.md.
- [x] (23/06/2026) Auditar ploomes_deals e events para a mesma falha de reconciliacao de Orders (registros orfaos). AUDITORIA FEITA (23/06/2026, subagente ploomes-verifier, read-only no banco de dev): a falha-modelo (chave de upsert reatribuivel pelo Ploomes) NAO se aplica a `ploomes_deals` nem a `events` — ambos sao chaveados por `ploomes_deal_id`, que o Ploomes nao reatribui. Zero orfaos de FK, zero duplicatas de chave natural, zero divergencia de unit_id, zero cross-wiring events<->deals. Onde o padrao se aplica de fato e em `ploomes_order_products`, e o fix (DELETE WHERE order_id antes do upsert, commit 8b8c589) esta presente e funcionando. UNICO ACHADO (ALTA): 1 ghost-row residual na order `601460638` (deal Ganho) — copia dupla das 9 linhas de produto (set 1 00:17 / set 2 20:30 de 17/04 com product_id reatribuido); SUM(total)~R$215,2k ~= o dobro do real; infla o BI em ~R$107,6k. Escapou da limpeza manual de 17/04/2026 (que tratou so 3 orders Type B). FIX: re-sync da order (o sync atual faz DELETE+reinsert e corrige; NAO deletar a mao). STATUS: RESOLVIDA (23/06/2026, confirmado em PROD). A order 601460638 ja estava LIMPA em producao (9 produtos, SUM~R$107,6k, sem ghost-row) — o achado de ~R$215,2k era artefato do snapshot de DEV (semeado num momento com a duplicata). Varredura ampla na PROD (fingerprint de linhas duplicadas + spread de created_at): NENHUM ghost-row real. Os 22 orders com linha duplicada tem TODOS spread_min=0 (linhas criadas na mesma rodada de sync), ao contrario da assinatura de ghost-row (2 rodadas com horas de intervalo, como no dev). Sao duplicatas legitimas (item de linha repetido, ex. Taxa de Rolha x2 — sobrevivem ao onConflict por terem ploomes_product_id distintos) ou a divergencia documentada soma-produtos-vs-amount (preco tabela vs negociado), nao a falha de chave reatribuivel. ploomes_deals/events NAO sofrem a falha-modelo (chaveados por ploomes_deal_id, que o Ploomes nao reatribui). Ferramenta nova `scripts/resync-order-products.ts` (dry-run/--apply) deixada no repo para incidentes futuros de ghost-row (corrigida para ler a user_key do ploomes_config e a data do banco; nao depende de env). Nenhuma correcao foi necessaria em producao.

## Produto / Features pendentes

- [x] (24/06/2026, prod 2444fc1) Atas — Minhas Tarefas Fase B + Fase C. Ver entrada completa em "Resolvidas".
- [ ] Decoracao Fase B (3 layouts + 18 APIs, sem SQL). STATUS: BLOQUEADA ate o Bruno dizer "Fase 2 congelada".
- [ ] Submodulo de Baloes. STATUS: AGUARDANDO TERCEIRO (a Mari responder sobre unificar em Itens).
- [ ] Vendas Fase 3 / Orders Fase 2 — UI Vendedoras + melhorias de BI por Categoria + vendedoras ativas/inativas. STATUS: PENDENTE.
- [x] (24/06/2026, prod 9b409f0) Central de Servicos Fase 2 — anexos + confirmacao de leitura (dashboard NAO feito a pedido do Bruno; hub removido). Ver entrada completa em "Resolvidas".
- [x] (23/06/2026, prod e274e70) Permissoes de Custo de Prestador (event_providers.agreed_price). FEITO em 22/06/2026 o gate de EXIBICAO no frontend: novo `ROLES_CAN_VIEW_PROVIDER_COST` = [super_admin, diretor, gerente, financeiro] + `canViewProviderCost` em roles.ts; aplicado em EventProviderCard ("Restrito"), ProvidersSection (oculta total) e EventHistorySection (oculta linha). Revisado pelo rbac-auditor (APROVADO COM RESSALVAS). FOLLOW-UPS pendentes (achados do rbac-auditor): (1) o `agreed_price` ainda TRAFEGA no payload de rede — EVENT_PROVIDER_SELECT/PROVIDER_EVENT_SELECT usam `*`; o gate e so visual, quem abrir DevTools le o valor. Fechar de verdade exige omitir a coluna do SELECT por role OU RLS/RPC/coluna mascarada. (2) o WRITE do custo no AddProviderModal NAO e gateado ("nao ve, mas escreve") — assimetria a resolver. (3) RESOLVIDO 22/06: conjunto que VE o custo = gestao (super_admin/diretor/gerente) + financeiro + operacionais que contratam prestador (manutencao, decoracao); bloqueadas vendas (vendedora/pos_vendas). Isso tambem alinha a assimetria do write p/ manutencao/decoracao (agora veem o que definem). (4) FEITO 23/06/2026: gate do WRITE no AddProviderModal — bloco "Valor acordado"/"Cobranca" so renderiza quando canViewProviderCost(role) e o submit nao envia agreed_price p/ quem nao ve o custo (fecha o "nao ve mas escreve" de vendedora/pos_vendas). Validado via subagente visual-qa (gerente ve o bloco; vendedora nao ve; sem /403; gate tambem no submit). (5) DECISAO 23/06/2026: a parte do payload de rede (agreed_price ainda trafega via SELECT *) foi ACEITA como risco de baixa severidade — exposicao apenas interna (funcionario com DevTools), RLS do Postgres e row-level e o fechamento server-side exigiria view/RPC mascarada + revoke do SELECT direto + rewire dos hooks de leitura (desproporcional ao risco hoje; reabrir se virar prioridade). STATUS: RESOLVIDO — write-gate (parte 4) em prod 23/06/2026 (commit 4601b44, merge adf171e, deploy manual). Parte (1)/payload de rede = risco aceito documentado (nao sera fechado salvo reprioritizacao).
- [ ] Infra/Dev — cron ploomes-sync nao escreve no banco de dev: o createAdminClient() usa service_role key, mas o PostgREST do Supabase self-hosted no dev retorna 42501 (permission denied for table users) mesmo com essa key — quirk conhecido do ambiente de dev (nao afeta producao). Workaround usado na Fase 2b-1: backfill manual via psql -U postgres (superuser, bypassa PostgREST). Nao ha acao urgente; registrado para rastreabilidade caso seja investigado em rodada futura. NOTA (25/06/2026): o mesmo quirk apareceu no backfill do contrato assinado (service_role sem GRANT em ploomes_orders/events no dev) — resolvido no dev com GRANT manual; em PROD o service_role tem os grants padrao e o backfill rodou sem erro.
- [ ] Webhook do Ploomes so escuta Deal — contrato assinado (Clicksign) atualiza ate ~30 min depois (v1.67.0, 25/06/2026). O campo "Assinado (Clicksign)" e da entidade ORDER, mas registramos webhook so para Deal (webhook-register/route.ts tem DEAL_ENTITY_ID=2 fixo; o handler webhooks/ploomes/route.ts so processa Deal). Por isso assinar um contrato (edicao de Order) nao chega em tempo real — quem propaga e o cron ploomes-sync-orders (*/30, incremental por LastUpdateDate), entao a etiqueta "Assinado" aparece em ate ~30 min e se auto-resolve. NAO e limitacao do Ploomes: ele suporta webhook de Order (EntityId=4, ActionId=2 Update). MELHORIA (opcao B do chat): para ficar quase instantaneo — (1) generalizar webhook-register para registrar EntityId=4/Update; (2) criar syncSingleOrder(orderId) que extrai contract_signed e chama refreshEventContractSignedFromOrders; (3) adicionar o ramo Order no handler do webhook (validar ValidationKey, idempotencia, retornar 200 rapido). Mudanca MODERADA + nova superficie de bug — merece plano e revisao dos subagentes ploomes/security. ATALHO BARATO se 30 min incomodar: baixar o cron de orders para */10 ou */15 no crontab da VPS (so config, sem codigo). STATUS: PENDENTE (baixa prioridade — Bruno decidiu manter o comportamento atual; abrir so se "instantaneo" virar requisito).

## Pequenas / Housekeeping

- [ ] Recriar os 7 usuarios de teste de producao (teste-{role}@cachola.cloud) removidos em abril/2026 (~30 min). STATUS: PENDENTE.
- [ ] Tipagem do nodemailer desatualizada apos o bump 6->9 (25/06/2026). Ha um shim caseiro `src/types/nodemailer.d.ts` rotulado "v6" (`declare module 'nodemailer'`) que sombreia o `@types/nodemailer@^6.4.23` ainda em devDependencies. Como o shim so descreve a API estrutural usada (host/port/secure/auth/from/to/subject/html/text), cobre a v9 sem ajuste e o tsc passa limpo — por isso e so cosmetico. Limpeza futura: atualizar o comentario do .d.ts para "v9" e considerar remover o `@types/nodemailer` v6 orfao. STATUS: PENDENTE (cosmetico, sem impacto funcional/seguranca; apontado pelo security-reviewer).
- [ ] Central de Servicos / anexos de avisos — 3 trade-offs aceitos (baixa severidade, documentados pelos subagentes na Fase 2, 24/06/2026): (a) **orfaos no storage se a aba fechar com upload pendente** — o edit-sheet limpa pendentes no Cancelar/X (best-effort fire-and-forget), mas fechar a aba/recarregar deixa o objeto no bucket `central-servicos-avisos-anexos`; mesma classe do debito de orfaos da Decoracao (CLAUDE.md); fix futuro = cron varrendo o bucket por objetos sem linha em `central_servicos_aviso_anexos`. NOTA: o DELETE do anexo e o DELETE do aviso JA limpam os objetos (so o tab-close fica de fora). (b) **`size_bytes` so validado no client** — cosmetico; o tamanho real e barrado pelo `file_size_limit` do bucket. (c) **a SELECT-policy do storage gateia so por `view`, nao re-checa a vigencia do aviso** — mitigado pois o `storage_path` (UUID, nao-enumeravel) so chega ao client via a RLS da tabela; mesmo trade-off ja aceito no bucket de contatos (mig 146). STATUS: ACEITOS (reabrir so se virar prioridade).
- [x] (22/06/2026) Alinhamento de sinal de gap: eventos eram o outlier (usavam <= 0 p/ sobreposicao e > 0 p/ intervalo curto). Alinhados ao padrao da RPC (mig 049/154) e do use-pre-reserva-conflicts: sobreposicao = `gap < 0`, intervalo curto = `0 <= gap < 120`. gap=0 (encostado) agora conta como "intervalo < 2h", nao sobreposicao — consistente nos dois lados. Mudanca client-only em use-event-conflicts.ts (useOverlappingEventIds + useShortGapEventIds).
- [x] (23/06/2026) Environment "VPS_SSH_KEY" no GitHub. VERIFICADO via `gh api repos/.../environments`: existe apenas o environment `producao` (total_count=1). Nao ha environment `VPS_SSH_KEY` — nada a apagar. Sem acao.
- [x] (22/06/2026) Seguranca — /api/audit: reforco do payload FEITO. `validateAuditField` trava old_data/new_data (rejeita nao-objeto, >100 chaves, >16KB serializado); entity_type/entity_id ganharam teto de 200 chars; ip (x-forwarded-for) passou a pegar so o 1o IP da lista + validar formato (evita INSERT INET quebrar e perder a trilha). Revisado pelo subagente security-reviewer (APROVADO COM RESSALVAS — os 2 vizinhos entity_type/ip foram incorporados no mesmo commit). NAO implementado de proposito: allowlist de chaves POR entity_type (custaria um registry por tipo; o trio forma+tamanho+allowlist de action/module ja cobre o vetor de forja por autenticado).

## Seguranca / LGPD

> Varredura AppSec profunda em 25/06/2026 (4 instancias do appsec-security-reviewer sobre auth/acesso, APIs/service_role/config, RLS/RPC/SQLi, input/upload/webhook/XSS). Fundacao SOLIDA: defense-in-depth real, service_role contido no servidor, zero segredo no historico, RLS universal, zero SQLi. Achados criticos da onda 1 (proxies Ploomes + webhook) JA RESOLVIDOS (ver Resolvidas). Os itens abaixo sao os 🟡 deferidos para ondas seguintes.

- [x] (25/06/2026, develop) **Open redirect no login** (`login/page.tsx`) — A01. RESOLVIDO: helper `safeInternalPath` saneia `redirectTo` na origem (so caminho interno; `//`, `/\`, absoluto → `/dashboard`). Re-revisado pelo appsec (APROVADO; `useSearchParams().get()` ja URL-decodifica → sem bypass por encoding).
- [x] (25/06/2026, prod 4e82bf8) **`get_event_conflicts` / `get_conflicting_event_ids` (migration 027→167)** — A01 cross-tenant. RESOLVIDO E EM PRODUCAO: migration 167 (`CREATE OR REPLACE` com guard `is_global_viewer() OR p_unit_id = ANY(get_user_unit_ids())` + `SET search_path = public`; gemea herda via ALTER) aplicada via esteira migrate-prod.yml (dry_run verde → apply real verde; backup pg_dump 230MB pre-migracao; healthcheck 307; registrada em cachola_migration_log). Validada por migration-reviewer + appsec + dry_run contra schema real de prod.
- [x] (25/06/2026, develop) **Vazamento de `error.message`/`error.code` ao cliente** (A10) em `admin/backups`, `ploomes/sync/status`, `ploomes/config` — RESOLVIDO: `console.error` interno + `{ error: 'Erro interno.' }` generico; removido `detail`/`code` da resposta do config. Re-revisado pelo appsec (APROVADO; grep confirmou zero `.message`/`.code`/`.stack` ao cliente).
- [x] (25/06/2026, develop) **`ploomes/sync/status`** — A01. RESOLVIDO: `requireRoleApi(SETTINGS_ROLES)` antes do `createAdminClient` (era so `getUser()`). Re-revisado pelo appsec (APROVADO).
- [x] (25/06/2026, develop) **PostgREST filter injection em `.or()`** (robustez, NAO bypass de tenant — RLS continua valendo) nos 5 hooks (use-maintenance, use-suppliers, use-stage-drilldown-deals, use-bi-seller-deals, use-tickets). RESOLVIDO: helper `orIlike()` em `src/lib/utils/supabase-filters.ts` neutraliza `,()\\` antes de interpolar. Re-revisado pelo appsec (APROVADO; `LIKE` do Postgres sem backtracking → wildcards `%`/`*` fora do saneamento sao seguros).
- [ ] **MFA + rate limit no login** (A07) — decisao ARQUITETURAL, nao fix de uma linha. Hoje so o rate limit padrao do GoTrue; sem MFA nem rate limit por IP/device. Recomendado: MFA (TOTP) obrigatorio ao menos para super_admin/diretor; rate limit por IP no edge (Nginx limit_req na rota de auth do Kong) ou CAPTCHA adaptativo. STATUS: PENDENTE — **proposta escrita em `docs/seguranca/proposta-mfa-rate-limit.md`** (decisao Bruno 26/06: adiar impl, virar sessao dedicada). Sequencia recomendada: (1) rate limit Nginx (infra, rapido, reversivel), (2) MFA TOTP opt-in, (3) MFA obrigatorio p/ super_admin/diretor com fluxo de recovery.
- [ ] **Validacao de upload por magic bytes** (opcional, defense-in-depth) — o guard de MIME em `ploomes/upload` confia no `file.type` declarado pelo client (forjavel). Aceito como suficiente hoje (superficie restrita a SETTINGS_ROLES, destino e a API do Ploomes, nao serve de volta na origem). Endurecer com assinatura real (`%PDF-`, `\x89PNG`, `\xFF\xD8\xFF`) so se o consumidor mudar. STATUS: ACEITO (registrado).

- [ ] **Defense-in-depth opcional: REVOKE do GRANT de UPDATE na coluna `users.role` para `authenticated`** (A01) — 🟡 apontado pelo appsec como camada extra ao trigger da mig 171. Hoje a coluna `role` ainda e gravavel no nivel de GRANT (o trigger e a unica barreira). Cuidado: o GRANT atual e table-level, entao o REVOKE exige re-conceder explicitamente as colunas nao-privilegiadas (deny-by-default) e validar no dev p/ nao quebrar o self-update de perfil. STATUS: PENDENTE (baixa prioridade — o trigger ja fecha o vetor; reabrir so se quiser belt-and-suspenders).
- [ ] **`users.email` (e outras colunas de perfil) nao protegidas pelo trigger 171** (A01, higiene) — o trigger so protege role/is_active/seller_id. `email` em `public.users` e um espelho (login vive em `auth.users`/GoTrue), entao mudar `public.users.email` NAO e vetor de takeover; e so higiene de dados. STATUS: ACEITO/baixa (incluir no trigger so se virar relevante).
- [ ] **Promover CSP de Report-Only para enforce** (A02) — a frente atual entregou `Content-Security-Policy-Report-Only` permissiva (PR1). 🟡 do appsec: `script-src` com `unsafe-inline/unsafe-eval` e `connect-src` curinga (`https: wss:`) sao aceitaveis em Report-Only, mas precisam virar nonces + allowlist antes do enforce, senao a CSP nasce inutil. STATUS: PENDENTE (depende de observar os reports no console/Sentry e tunar com Next/PWA/Recharts/Sentry).

## Resolvidas

(Mover itens concluidos para ca, com data e versao. Exemplo: "- [x] (17/06/2026, v1.x) descricao do item.")

- [x] (26/06/2026, prod `7914d41`, v1.70.1) **Quick wins do backlog (2 itens).** (A) **Role-gate do SELECT/INSERT em `checklist-photos`** (Seguranca/LGPD, follow-up do C4): mig 172 troca `auth.uid() IS NOT NULL` por `auth_user_has_role(OPERATIONAL_CHECKLIST_ROLES)` nas policies de SELECT e INSERT do bucket (reusa o helper da mig 168) — fecha o residual cross-tenant (qualquer autenticado mintava signed URL / subia foto sabendo o path semi-previsivel). DELETE segue owner-based. Revisado por migration-reviewer + appsec (APROVADO). (B) **Refactor `useCreateUser`**: a criacao de usuario inline em `admin/usuarios/novo` virou hook em `src/hooks/use-users.ts`, uniformizando com `useUpdateUser`/`useDeactivateUser`; preserva `refetchType:'all'` (fix v1.69.1) e a UX de erro (msg do servidor vs "Erro inesperado"). Revisado por data-fetching (APROVADO). Deploy automatico verde (sem throttle dessa vez); mig 172 via esteira (dry_run→real).

- [x] (26/06/2026, prod `bb76552`, v1.70.0) **Frente Seguranca/LGPD — bloco inteiro do DIVIDAS tratado e EM PRODUCAO.** Consolidada via develop (PR #61), deploy manual do Bruno (auto throttlou), 4 migrations via esteira (ordem 171→168→170→169, dry_run→real, environment `producao`). 7 itens resolvidos:
  - **🔴 mig 171 — auto-escalada de cargo (A01, escalada de privilegio).** DESCOBERTO pelo appsec ao revisar a 168: a RLS `users: self update` restringe a LINHA mas NAO as colunas + `authenticated` tem GRANT de UPDATE em `users.role` → qualquer logado se autopromovia a super_admin via `PATCH /rest/v1/users?id=eq.<self>` direto no PostgREST. FIX: trigger BEFORE UPDATE `protect_users_privileged_columns()` (barra role/is_active/seller_id sem `check_permission(...,'usuarios','edit')`; SECURITY INVOKER de proposito). Testado (vendedora/gerente→42501; diretor→OK; self-update de perfil→OK).
  - **mig 168 — IDOR de storage (A01).** provider-documents/equipment-photos/checklist-comment-photos so checavam `auth.role()='authenticated'`. FIX: 12 policies role-based (4×3) via helper novo `public.auth_user_has_role(text[])` (molde `is_global_viewer`); role-sets espelham `src/config/roles.ts` (PRESTADORES/MAINTENANCE_MODULE/OPERATIONAL_CHECKLIST). Sem mudanca de app.
  - **mig 170 — guard interno nas RPCs de estoque da Decoracao (A01, defense-in-depth).** `PERFORM check_permission_or_raise('decoracao','edit')` no topo das 5 RPCs (criar/receber/cancelar_transferencia, encerrar_festa_decoracao, resolver_quarentena). Acao uniforme `edit` (nenhum cargo tem create sem edit). RLS das tabelas-alvo ja garantia o acesso; o guard e camada extra.
  - **mig 169 — bucket `checklist-photos` publico→privado (A01, PII de menor / C4).** Era `public=TRUE` (mig 007) → fotos legiveis por URL publica sem auth. FIX: `public=false`; app grava PATH + exibe via signed URL (`useSignedUrls` + helper `toChecklistPhotoPath`); conserta bug latente do PDF. Backfill em prod = no-op (0 fotos legadas).
  - **app-layer (sem migration):** allowlist de role anti mass-assignment + trava super_admin nos endpoints irmaos `change-role`/`apply-role-template` (helper `assertAssignableRole`; `route.ts` refatorado) — A01/A08; **anti-enumeracao no login** (`classifyError` colapsa "email not confirmed" na msg generica) — A07; **HSTS** (so prod) + **CSP-Report-Only** no `next.config.ts` — A02 (confirmados nos headers de prod).
  - Tudo revisado pelos subagentes (appsec obrigatorio, migration/rbac/data-fetching/design-tokens/visual-qa). Decisao do Bruno: manter "super_admin so por super_admin". Detalhe em [[project-seguranca-lgpd-frente]]. Follow-ups (nao-bloqueantes) ficaram na secao Seguranca/LGPD: REVOKE de coluna `users.role`, CSP enforce, role-gate do SELECT de checklist-photos. (Incidente do bucket publico avaliado pelo Bruno: NAO notificavel a ANPD — encerrado.)

- [x] (26/06/2026, prod f38017c, v1.69.0 + v1.69.1) **Processo de criacao de usuario consolidado em 1 tela / 1 submit** (antes 3 telas / 2 menus, com cargo escolhido 2x e podendo divergir, permissao via fire-and-forget e unidade padrao nao garantida). `/admin/usuarios/novo` agora coleta cargo + unidade(s) (multisselecao, 1 padrao); o endpoint `POST /api/admin/users` provisiona tudo server-side e AGUARDADO (convite + role/phone/seller_id + `user_units` com exatamente 1 `is_default` + `applyRoleTemplate` em escopo global `unit_id=null`). Unidade obrigatoria p/ cargos operacionais, opcional p/ super_admin/diretor (nova constante `UNIT_OPTIONAL_AT_CREATION_ROLES`). **Hardening AppSec (fechou 1 🔴 pre-existente):** allowlist de `role` em runtime (anti mass-assignment) + super_admin so por super_admin + erro de convite sem vazar `message` + `UPDATE users` dentro de try/catch; `requireRoleApi` passou a devolver o cargo do chamador (aditivo). v1.69.1 (follow-up): listagem atualiza sem F5 apos criar (`refetchType:'all'`) + colunas da listagem realinhadas (grid fixo + slot reservado p/ icones). Revisado por rbac/appsec/data-fetching/design-tokens; visual-qa via chrome-devtools. GOTCHA do CI: `npm run rbac:check` reprova comparacao literal de role (`=== 'super_admin'`) — usar `hasRole(SYSTEM_ONLY_ROLES)`. Deploy da v1.69.1 caiu no throttle de SSH → deploy manual do Bruno. Follow-up de seguranca aberto na secao Seguranca/LGPD (auditar endpoints irmaos).

- [x] (25/06/2026, prod 4e82bf8) Seguranca — **onda critica + app-layer + migration 167 da varredura AppSec, TUDO EM PRODUCAO** (merge develop→main 4e82bf8, deploy verde, migration 167 via esteira). Detalhe da onda critica abaixo.
- [x] (25/06/2026, prod 4e82bf8) Seguranca — **onda critica da varredura AppSec**. A varredura profunda (4 instancias do appsec-security-reviewer sobre docs/seguranca) achou 4 criticos, todos da mesma classe (proxy autenticado mas sem autorizacao por cargo + service_role). RESOLVIDOS em develop (commit 2912110): (1) `ploomes/deals`, (2) `ploomes/deals/[dealId]`, (3) `ploomes/upload/[dealId]` ganharam `requireRoleApi(SETTINGS_ROLES)` antes do `createAdminClient` — fecha o IDOR/PII (qualquer autenticado, incl. freelancer, lia/gravava CRM por ID); upload tambem ganhou validacao server-side de tipo/tamanho; (4) webhook `webhooks/ploomes` passou a fail-closed (chave ausente → 503) + `timingSafeEqual`. Decisao de cargo: SETTINGS_ROLES (nao VENDAS_MODULE_ROLES) por menor privilegio — consumidores sao so telas de config/admin. Re-revisado e APROVADO pelo appsec-security-reviewer; tsc limpo. Versionado tambem o subagente appsec + a skill docs/seguranca + REGRA DE SEGURANCA no CLAUDE.md (commit 5b31441). Os 🟡 deferidos (C4 checklist-photos, open redirect, get_event_conflicts cross-tenant, storage policies, error leak, HSTS/CSP, .or() injection, MFA) estao listados na secao Seguranca/LGPD acima. PENDENTE: merge develop→main (gate humano do Bruno) + validacao visual das telas de config que consomem ploomes/deals.

- [x] (25/06/2026, prod 2b24ced) Seguranca — **8 alertas HIGH do Dependabot ZERADOS** (confirmado via GitHub MCP: 0 high abertos). (1) **nodemailer** ^6.10.1 -> ^9.0.1 (direto): fecha raw-option file-read/SSRF (GHSA-p6gq-j5cr-w38f) + addressparser DoS (GHSA-rcmh-qjqh-p98v); API usada em `src/lib/email.ts` (createTransport/sendMail) e estavel v6->v9, vetores das CVEs nao exercidos. (2) **overrides** em package.json (junto dos ja existentes dompurify/fast-uri): hono ^4.12.25 (->4.12.27, fecha CORS-wildcard GHSA-88fw-hqm2-52qc, transitiva via CLI shadcn), @babel/plugin-transform-modules-systemjs ^7.29.4, serialize-javascript ^7.0.3, lodash ^4.18.0, **ws ^8** (fecha DoS do ws@7.5.10 que entrava so via @next/bundle-analyzer; o ws de runtime ja era 8.x) — os 4 ultimos sao build-time (next-pwa->workbox / babel). (3) **shadcn** movido dependencies -> devDependencies (CLI de dev). (4) **xlsx 0.18.5 -> 0.20.3** apontando para o tarball do CDN do SheetJS (o npm registry so tem a 0.18.5 abandonada; ver divida nova em Infra) — fecha ReDoS (CVE-2024-22363, >=0.20.2) + Prototype Pollution (CVE-2023-30533, >=0.19.3). xlsx e WRITE-ONLY no projeto (3 sitios em src/lib/bi/export-*.ts + src/lib/utils/export.ts; zero XLSX.read), entao as CVEs (que exigem parsear arquivo) eram inalcancaveis — o advisory do GitHub literalmente diz "exporting data to spreadsheet files are unaffected". Validado: tsc limpo, lint exit 0, npm run build (PWA/sw.js) verde, smoke nodemailer@9, security-reviewer APROVADO. npm audit em prod: 0 high (restam 10 moderate + 2 low, todos build/dev-time). 3 commits + 3 merges (sem bump de versao — sem mudanca user-facing). Restam dividas cosmeticas: ver nodemailer .d.ts em Housekeeping.
- [x] (25/06/2026, v1.66.0, prod 3a191c4) Limpeza de redundancia no detalhe da festa (/eventos/[id]) — 5 acordeons removidos apos os espelhos do Ploomes (Checklist do Cliente/Decoracao) e o acordeon Vendas terem coberto os dados. (1) **Briefing** (100% espelhado em "Briefing Inicial" no Checklist de Decoracao). (2) **Servicos Contratados** (6 booleans show/decoracao-alinhada/doces-decorados/lembrancinhas/bebidas-de-fora/foto-video, todos no Checklist do Cliente). (3) **Financeiro** (nao-linkado a nenhuma funcao; deal_amount ja aparece no acordeon Vendas via deal do Ploomes; payment_method NUNCA populado — 0/724 no snapshot de dev). (4) **Prestadores** e (5) **Checklists** (em desuso a pedido do Bruno; modulos /prestadores e /checklists permanecem no menu; o Historico foi preservado pois reusa useEventChecklists/EventTimeline). A remocao do Checklists tirou junto o botao "Checklists" da barra de acoes mobile e o entry-point "Criar Checklist" (CreateChecklistModal). Limpeza de imports/helpers/estado orfaos (BoolIndicator, EventChecklistRow, QuickActionsBar.onChecklistsClick, etc.). -185 linhas, tudo em src/app/(auth)/eventos/[id]/page.tsx. tsc+lint limpos, visual-qa APROVADO (desktop+mobile claro nas 5 remocoes), validado em prod pelo Bruno. Sobram so sobreposicoes parciais/curadas (Sabor do bolo, Musicas, horarios, Pai) que ficam de proposito.
- [x] (24/06/2026, v1.65.0, prod fa8e5d0) Checklist de Decoracao — novo acordeon read-only no detalhe da festa (/eventos/[id]), LOGO ABAIXO do "Checklist do Cliente" (icone Sparkles). Mesmo molde do Checklist do Cliente: espelha campos de DEAL do Ploomes, botao Imprimir em aba nova (window.open+document.write), "nao preenchido" para vazios. 16 campos: 7 reaproveitam colunas ja existentes em events (theme, decorator_name, setup_time, teardown_time, briefing, cake_flavor, has_decorated_sweets); 9 novos via migration 165 (7 text + 2 numeric: balloons_value, fake_cake_value). Os 2 valores (Baloes/Bolo Fake "Valor cobrado") sao gateados por canViewFestaValues (mostram "Restrito" para quem nao ve valores de festa). FieldKeys descobertos por scripts/discover-checklist-decoracao-fieldkeys.ts (read-only). Arquivos: EventChecklistDecoracaoSection.tsx + checklist-decoracao-print.ts + field-mapping.ts/types.ts/sync.ts/database.types.ts. Migration 165 aplicada em prod pela esteira #10 (dry_run + real OK). Sync completo do Ploomes popula as colunas ao longo dos ciclos (sem backfill dedicado — igual ao Checklist do Cliente da mig 162). 4 subagentes (migration/ploomes/design-tokens/visual-qa) APROVADOS. Validado em prod pelo Bruno (festa de hoje ja exibindo dados). DUAS LIMITACOES DE DADO DO PLOOMES (aceitas, baixa prioridade, ja contornadas no codigo): (a) "Forminhas - Cores" vem como CODIGOS (ex.: "10,6,9,26"), nao nomes de cor — exibimos o texto cru; corrigir exigiria mapear codigo->cor ou ajustar o preenchimento no CRM. (b) "Baloes - Valor cobrado" (deal_CA67DC60) e "Bolo Fake - Valor Cobrado" (deal_040C6AD0) estao DECLARADOS como TypeId 5 (Data/hora) no painel do Ploomes mas gravam DecimalValue/R$ na pratica — mapeados como DecimalValue/number (confirmado por amostragem de 50 deals); o conserto "certo" seria mudar o tipo do campo no painel do Ploomes, mas o contorno e seguro e nao corre risco de corromper o valor. Reabrir so se virar prioridade.

- [x] (24/06/2026, prod 9b409f0) Central de Servicos Fase 2 — Mural de Avisos. **E.1 Anexos** (prod dd98f88 + migration 163 via esteira #6): bucket privado `central-servicos-avisos-anexos` + tabela `central_servicos_aviso_anexos` (RLS espelhando a vigencia do aviso pai), uploader `FileDropZone` (PDF+imagem, multi-arquivo, sem compressao — distinto do PhotoDropZone que so faz imagem->JPEG), badge + lista clicavel via signed URL no card, API POST/DELETE de anexos (valida shape do path anti-traversal; DELETE limpa o objeto do storage; DELETE do aviso tambem limpa orfaos). **E.2 Confirmacao de leitura** (prod dd98f88 + migration 164 via esteira #8): coluna `exige_confirmacao` + tabela `central_servicos_avisos_leitura` (UNIQUE aviso+usuario, RLS: usuario ve/cria/apaga so a propria, gestor ve todas), Switch no autor, botao "Confirmo que li" -> "Voce confirmou em DD/MM", contagem + lista de quem confirmou para o gestor. **E.3 Dashboard — NAO ENTREGUE**: foi construida e validada (KPIs + pendencias + acompanhamento), mas o Bruno esclareceu que NAO queria tela inicial no modulo; entao foi REVERTIDA e o **hub foi removido** (prod 9b409f0): `/central-servicos` agora redireciona para `/central-servicos/avisos`, sem landing propria (o item do menu lateral so expande os filhos). Cada entrega passou por 4 subagentes (migration/rbac/security/data-fetching) + visual-qa, e foi validada ponta-a-ponta no dev E no prod (Bruno). Deploys: E.1 manual (throttle SSH), E.2 e remocao do hub automaticos. Migrations 163/164 aplicadas via esteira migrate-prod.yml.
- [x] (24/06/2026, prod 2ac9911) Templates de e-mail nao escapavam input de usuario (HTML injection) — TRANSVERSAL, pre-existente. RESOLVIDO em prod. Helper `escapeHtml()` adicionado em `base.ts` (escapa `& < > " '`) e aplicado a TODA interpolacao de campo livre nos 8 templates de `src/lib/email-templates/` (action-item-assigned, meeting-minute-notification, event-tomorrow, checklist-overdue, maintenance-overdue, maintenance-emergency, generic-notification, backup-failure). `wrapInLayout` escapa `title` e `preheader` de forma centralizada; o `body` (HTML ja composto) e as URLs (paths/IDs controlados) NAO sao escapados. Sem regressao visual: campos escapados sao texto plano (nomes/titulos/descricoes) — acentos/emoji intactos, e um `&` legitimo renderiza certo no cliente de e-mail. Severidade original BAIXA (e-mail, nao DOM; so staff com permissao de escrita injeta). commit a405d62, merge 2ac9911, deploy automatico verde. (identificado 24/06/2026 pelos subagentes rbac-auditor + security-reviewer na Atas Fase B; nao era regressao da Fase B.)

- [x] (24/06/2026, prod 2444fc1) Atas — Minhas Tarefas Fase B + Fase C CONCLUIDAS e em producao. Fase A (tela /atas/minhas-tarefas + RPCs get_my_action_items/set_my_action_item_status, migration 157) ja existia (v1.54.0). **Fase C** (frontend-only, sem migration): nova fonte "Minhas Tarefas" no calendario do dashboard, reusando get_my_action_items via novo hook `use-calendar-action-items.ts` (compartilha o cache de useMyMeetingTasks, sem chamada extra; filtra due_date presente + oculta status=done). `calendar-view.tsx` ganhou a fonte indigo (icone ListTodo) espelhando o padrao de checklist em TODAS as views (Mes/Semana/Dia/Lista/popover), chip de filtro "Minhas Tarefas" persistido em localStorage, click -> /atas/{id} se can_view_full senao /atas/minhas-tarefas. `dashboard/page.tsx` liga a prop actionItemsData. **Fase B** (sem migration, reusa create_notification): notifica o responsavel (sino + e-mail) quando uma action item lhe e atribuida na criacao/edicao de uma ata, por TRANSICAO de atribuicao (item novo com responsavel OU assigned_to mudou vs. original; re-salvar nao re-notifica; auto-atribuicao excluida). Arquivos: template `action-item-assigned.ts` + route handler `/api/minutes/notify-assignment` (guard atas:edit, ENDURECIDO apos auditoria rbac+security: autor via auth.uid() nao do payload, destinatario validado contra meeting_action_items.assigned_to da ata — fecha destinatario arbitrario + vazamento de titulo cross-unidade) + wiring fire-and-forget em `use-meeting-minute-mutations.ts` + icone ListTodo no `notification-bell.tsx`. Subagentes: data-fetching APROVADO; rbac+security APROVADO COM RESSALVAS (achado medio CORRIGIDO no endurecimento; escape de HTML nos templates registrado como divida transversal na secao Seguranca/LGPD acima). visual-qa APROVADO (pills/dots indigo, navegacao, toggle, dark mode, popover em desktop+mobile). Commits 8f1e157 (C) + 4e3ef27 (B), merge 2444fc1. CI verde, deploy automatico, prod build-info=2444fc1.
- [x] (19/06/2026, v1.63.1 + v1.63.2; confirmado em prod 22/06/2026) Checklist do Cliente — rollout em producao (Fase 2b-3) CONCLUIDO. Sequencia executada: merge develop->main -> deploy do codigo (v1.63.1 "Fase 2b completa: encanamento + interface + layout", commit b4a8b91, 19/06 21:43; v1.63.2 "impressao em aba nova + rodape", commit d177bdf, 19/06 23:19) -> esteira migrate-prod.yml aplicou a migration 162 em producao (run #27850755436, success em 19/06 22:12, entre as duas releases, padrao deploy-primeiro respeitado). Confirmacao em prod (22/06/2026): as 9 colunas novas em public.events (corkage_quantity, extra_guest_staff_value, responsible_person, photo_video_contact, generator, valet_cost, checklist_other_details, corkage_paid, overtime_details) existem no schema de producao (9/9). Nada pendente desta frente.

- [x] (19/06/2026, v1.63.0-dev) Checklist do Cliente — Fase 2b (encanamento + interface): 9 FieldKeys do Ploomes mapeados, migration 162 aplicada no dev, DEAL_FIELD_MAP + ParsedDeal + Event type + sync.ts atualizados, buildChecklistClienteItems agora exibe todos os 21 campos com dados reais (antes 9 eram placeholders fixos). Fix do bug de foto/video da Fase 1 (item 13 "Contratou foto/video?" estava em NAO_PREENCHIDO; item 14 "Contato(s) foto/video" apontava para campo errado). Campo financeiro "Valor do Convidado Extra e Staff" protegido por canViewFestaValues (exibe "Restrito" para cargos sem acesso). Validado via Chrome DevTools (admin + rh) no dev. Falta apenas o rollout em producao (ver item acima).
- [x] (18/06/2026, dev-only) operacional_eventos — seed de usuarios de teste corrigido para popular user_permissions a partir de role_permissions (fonte de verdade, mesma usada por applyRoleTemplate no fluxo real) em vez de role_default_perms (legado, incompleto). Correcao aplicada em scripts/seed-local-test-users.ts — usuarios de teste agora recebem dashboard.view e eventos.view ao ser criados, eliminando o /403 no login do dev. Validado via MCP Chrome DevTools: teste.operacional_eventos@cachola.local logia, abre /dashboard e ve o item "Agenda" em Manutencao. Producao NAO foi alterada (Fernanda e demais usuarios reais ja usavam o fluxo correto via applyRoleTemplate). Opcao A (backfill em producao) nao necessaria pois nao ha usuarios operacional_eventos em producao ainda.
- [x] (17/06/2026, v1.56.1) Auto-retry 3x no passo Deploy via SSH (deploy.yml) — resolve as falhas transitorias de rede GitHub->VPS que vinham forcando deploy manual.
